### 1. What is the difference between getElementById, getElementsByClassName, and querySelector / querySelectorAll?
Ans: getElementById can call ID only and a single element only.
     getElementsByClassName can call className only and get a HTML collection.
	 querySelector get all matching element and change first element only. It can call and change any CSS selector.
	 querySelectorAll get NodeList can change all items. It can call and change any CSS selector.

### 2. How do you create and insert a new element into the DOM?
Ans: DOM can create element. can add or remove content ot attributes. and add into DOM.

### 3. What is Event Bubbling? And how does it work?
Ans: Bubbling start tageted elements and move to parent elements.

### 4. What is Event Delegation in JavaScript? Why is it useful?
Ans: Event Delegation work with event bubbling to handle events at parents elements instead of every children

### 5. What is the difference between preventDefault() and stopPropagation() methods?
Ans: preventDefault() work to stops browser default behavior.
     stopPropagation() work to stops event bubbling.

